# これは何
ExpressベースのTypeScriptサーバのPlayground

# 初期構築
```
// npmプロジェクト作成
npm init -y

// expressインストール
npm install express

// 開発環境用にTypeScriptをインストール
npm install -D typescript @types/express @types/node
// ライブリロード用
npm install -D ts-node nodemon
```

# 環境構築